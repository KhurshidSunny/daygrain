/**
 * Analytics and AdSense load from root layout via next/script when you set env vars.
 * Do not render <script> tags inside React components (causes Next.js console warning).
 */
export function IntegrationPlaceholders() {
  return null
}
