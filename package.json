{
  "name": "daygrain-hub",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "next dev --webpack",
    "dev:hub": "next dev --webpack",
    "dev:turbo": "next dev",
    "dev:focus": "npm --prefix tools/focus run dev",
    "dev:decide": "npm --prefix tools/decide run dev",
    "dev:colors": "npm --prefix tools/colors run dev",
    "dev:data": "npm --prefix tools/data run dev",
    "build:focus": "node tools/focus/clean-install.mjs && npm --prefix tools/focus run build",
    "build:decide": "node tools/decide/clean-install.mjs && npm --prefix tools/decide run build",
    "build:colors": "node tools/colors/clean-install.mjs && npm --prefix tools/colors run build",
    "build:data": "node tools/data/clean-install.mjs && npm --prefix tools/data run build",
    "build": "npm run build:focus && npm run build:decide && npm run build:colors && npm run build:data && next build",
    "start": "next start",
    "lint": "eslint"
  },
  "dependencies": {
    "lucide-react": "^1.17.0",
    "next": "16.2.6",
    "react": "19.2.4",
    "react-dom": "19.2.4"
  },
  "devDependencies": {
    "@tailwindcss/postcss": "^4",
    "@types/node": "^20",
    "@types/react": "^19",
    "@types/react-dom": "^19",
    "eslint": "^9",
    "eslint-config-next": "16.2.6",
    "tailwindcss": "^4",
    "typescript": "^5"
  }
}
